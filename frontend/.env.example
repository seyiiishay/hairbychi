# Copy to .env.local for local development. Set the real values in
# Vercel/Lovable project settings for staging & production — never commit
# a real .env file. For Railway, point this at the host root WITHOUT /api.

VITE_API_BASE_URL=https://appoinment-system-webapp-staging.up.railway.app
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
VITE_CAPTCHA_SITE_KEY=
VITE_CAPTCHA_PROVIDER=hcaptcha
VITE_PUBLIC_SITE_URL=http://localhost:5173
VITE_APP_ENV=development
